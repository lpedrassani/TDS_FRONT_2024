const nome_capturado_input = document.getElementById("nome");
const email_capturado_input = document.getElementById("email");
const msg = document.getElementById("msg");

function mensagem()
{
    msg.innerHTML="Muito obrigado(a), senhor(a)" [nome_capturado_input], "acabamos de enviar um e-mail para: " [email_capturado_input];
}